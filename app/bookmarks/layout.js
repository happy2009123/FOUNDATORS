export const metadata = {
  title: 'Saved Posts — Foundators',
  description: 'Your bookmarked posts and saved content on Foundators.',
};

export default function BookmarksLayout({ children }) {
  return <>{children}</>;
}
